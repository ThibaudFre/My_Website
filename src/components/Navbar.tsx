import { NavBarLinks } from "@/types";
/*Prop need to contain one href and one name so

*/

export default function Navbar(links : NavBarLinks) {
    console.log(links);
    return (
            <ul>
                
            </ul>
    )
}